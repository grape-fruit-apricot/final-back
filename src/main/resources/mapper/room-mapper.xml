<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.midpoint.room.model.dao.RoomMapper">

	<insert id="insertRoom" parameterType="Room">
		INSERT 
		  INTO 
		  	   ROOM (
					  ROOM_ID
					, ROOM_UUID
					<if test="maxParticipants != null">
					, MAX_PARTICIPANTS
					</if>
					)
		VALUES 
			   (
				 SEQ_ROOM.NEXTVAL
			   , #{roomUuid}
			   <if test="maxParticipants != null">
			   , #{maxParticipants}
			   </if>
			   )
	</insert>

	<update id="updateMidpoint" parameterType="Room">
		UPDATE
			   ROOM
		   SET
			   MIDPOINT_LAT = #{midpointLat}
			 , MIDPOINT_LNG = #{midpointLng}
			 , MIDPOINT_SOURCE = #{midpointSource}
		 WHERE
			   ROOM_ID = #{roomId}
	</update>

	<update id="updateStage" parameterType="Room">
		UPDATE
			   ROOM
		   SET
			   STAGE = #{stage}
		 WHERE
			   ROOM_ID = #{roomId}
	</update>

	<!--
		findRoom 과 같지만 방 행을 잠근다. 여러 명이 동시에 같은 방을 바꾸려 할 때
		서로의 커밋을 보지 못해 아무도 마지막 한 명이 되지 못하는 문제를 막는다.
		반드시 쓰기 트랜잭션 안에서만 호출한다.
	-->
	<select id="findRoomForUpdate" resultType="RoomResponseDto">
		SELECT
			   ROOM_ID
			 , ROOM_UUID
			 , MAX_PARTICIPANTS
			 , MIDPOINT_LAT
			 , MIDPOINT_LNG
			 , MIDPOINT_SOURCE
			 , STAGE
			 , CREATED_AT
			 , EXPIRES_AT
		  FROM
			   ROOM
		 WHERE
			   ROOM_UUID = #{roomUuid}
		   FOR UPDATE
	</select>

	<select id="findRoom" resultType="RoomResponseDto">
		SELECT
			   ROOM_ID
			 , ROOM_UUID
			 , MAX_PARTICIPANTS
			 , MIDPOINT_LAT
			 , MIDPOINT_LNG
			 , MIDPOINT_SOURCE
			 , STAGE
			 , CREATED_AT
			 , EXPIRES_AT
		  FROM 
		  	   ROOM
		 WHERE
		 	   ROOM_UUID = #{roomUuid}
	</select>

	<!--
		만료된 방을 한 번에 다 읽지 않고 상한을 둔다. 방 하나가 경로점까지 수천 행이라
		한 트랜잭션에 다 담으면 Oracle XE 의 undo 와 커넥션을 오래 잡는다.
		남은 방은 다음 주기에 이어서 지운다.

		만료 판정에 자바 시각을 넘기지 않고 SYSDATE 를 쓰는 이유는, EXPIRES_AT 자체가
		SYSDATE + 3/24 로 들어가기 때문이다. 앱 서버 시계가 DB 와 다르면 두 값의 기준이 갈린다.
	-->
	<select id="findExpiredRoomIdList" resultType="long">
		SELECT
			   ROOM_ID
		  FROM
			   ROOM
		 WHERE
			   EXPIRES_AT &lt; SYSDATE
		   AND ROWNUM &lt;= #{batchSize}
	</select>

	<!--
		자식 테이블은 지우지 않는다. FK 가 전부 ON DELETE CASCADE 라 방 한 줄을 지우면
		참가자·식당·선택·투표·게임·경로·추적 이력까지 DB 가 함께 정리한다.
		여기서 자식을 직접 지우면 지워야 할 테이블이 늘어날 때마다 이 SQL 을 고쳐야 한다.
	-->
	<delete id="deleteRoomList">
		DELETE
		  FROM
			   ROOM
		 WHERE
			   ROOM_ID IN
			   <foreach item="roomId" collection="list" open="(" separator="," close=")">
			   #{roomId}
			   </foreach>
	</delete>

</mapper>
