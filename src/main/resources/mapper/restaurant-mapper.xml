<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.midpoint.restaurant.model.dao.RestaurantMapper">

	<insert id="insertRestaurant" parameterType="Restaurant">
		INSERT 
		  INTO 
		  	   RESTAURANT (
							RESTAURANT_ID
						  , ROOM_ID
						  , NAME
						  , SOURCE
						  , ADDRESS
						  , LAT
						  , LNG
						  , ADDED_BY
						  , CREATED_AT
						  )
		VALUES 
			   (
				 SEQ_RESTAURANT.NEXTVAL
			   , (
				   SELECT 
				  		  ROOM_ID
				  	 FROM 
				  		  ROOM
				    WHERE 
				   		  ROOM_UUID = #{roomUuid}
				 )
			   , #{name}
			   , 'MANUAL'
			   , #{address}
			   , #{lat}
			   , #{lng}
			   , #{participantId}
			   , SYSDATE
			   )
	</insert>

	<select id="findRestaurantList" resultType="RestaurantResponseDto">
		SELECT
			   r.RESTAURANT_ID
			 , r.NAME
			 , r.SOURCE
			 , r.ADDRESS
			 , r.LAT
			 , r.LNG
			 , r.ADDED_BY
			 , r.CREATED_AT
		  FROM 
			   RESTAURANT r
		  JOIN 
		  	   ROOM rm
		  	ON 
		  	   rm.ROOM_ID = r.ROOM_ID
		 WHERE 
			   rm.ROOM_UUID = #{roomUuid}
		 ORDER 
			BY 
		   	   r.CREATED_AT
	</select>

</mapper>
